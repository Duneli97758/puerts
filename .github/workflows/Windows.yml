name: Windows

on: [push]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Insatll MSVC
      uses: ilammy/msvc-dev-cmd@v1
      env:
        toolset: 14.XX
        sdk: 10.0.10240.0
    - name: Build
      run: |
        cd unity\native_src
        ./make_win64.bat      
